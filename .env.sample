# Diun Dispatcher Configuration

# Apprise URLs for notifications (comma-separated)
# Examples: pover://userkey@apptoken, ntfy://ntfy.example.com/topic
APPRISE_URLS=

# Webhook secret for Diun -> Dispatcher authentication
WEBHOOK_SECRET=

# Coolify API configuration
# NOTE: do NOT name this COOLIFY_URL — Coolify reserves the COOLIFY_* namespace
# and injects COOLIFY_URL as the resource's own FQDN, which would override it.
COOLIFY_API_URL=https://coolify.example.com
COOLIFY_TOKEN=

# Dispatcher public URL (for deploy links in notifications)
DISPATCHER_URL=https://dispatcher.example.com

# Container ignore list (comma-separated container names to skip)
IGNORE_CONTAINERS=

# Redeploy matched images automatically (true/1/yes/on). Off by default: the
# dispatcher only notifies with a deploy link.
# To be notified when the deployment is FINISHED, add a Webhook notification in
# Coolify pointing to:
#   https://dispatcher.example.com/coolify-webhook?secret=<WEBHOOK_SECRET>
AUTO_DEPLOY=false
